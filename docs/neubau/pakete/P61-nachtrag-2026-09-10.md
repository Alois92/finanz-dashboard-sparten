# P61 — Nachtrag aus der Kontrolle (Fable, 10. September 2026), verbindlich

Gehört zur Karte `P61-migrationsprobe.md` und hat bei Widersprüchen Vorrang. Das Paket wird **jetzt**, nach P20, gebaut und nicht erst in M6.

- **Warum jetzt:** Die Probe auf echten Daten (Schuld A2neu in `docs/neubau/SCHULDEN.md`) ist die eigentliche Abnahme von M1 und muss vor P31 laufen. Codex baut das Skript, der Kopf fährt es gegen die Kopie aus CT 101.
- **Arbeitsort:** `C:\Users\lblet\dev\wt-p61-migrationsprobe`, Zweig `pkt/p61-migrationsprobe` (auf `neubau` nach P20, Schema-Version 14). Keine Commits, kein Push.
- **Interpreter:** `C:\Users\lblet\dev\finanz-dashboard-sparten\.venv\Scripts\python.exe -m unittest discover -s tests`, vorher `FINANZ_DB` auf eine Wegwerf-Datei unter `%TEMP%`. Kann die Auth-Suite in der Sandbox nicht starten, das im Bericht sagen und `tests/test_migrationsprobe.py` isoliert grün zeigen.
- **Echte API von `app/migrate.py`** (die Karte nennt veraltete Namen): `status(con)` liefert `{'aktuell','anstehend','basis'}`; `anwenden(con, sicherung, sicherung_pflicht=True)` führt alle anstehenden Migrationen aus und gibt die Liste der angewendeten Versionen zurück; `sicherung` ist ein Callable ohne Argumente, das eine Kopie zieht und deren Pfad zurückgibt. Es gibt kein `apply` und kein `pending`.
- **Zusätzlich vergleichen:** je Sparte und Jahr auch die `bereich_id` der Sparte (ZINA muss Bereich 2 haben, alle anderen Bereich 1); Anzahl Buchungen und Zeilen vorher/nachher; `PRAGMA foreign_key_check` leer; `PRAGMA integrity_check` ok; zweiter `anwenden`-Lauf leer; Inhalt von `migrationsprotokoll` (Migration 012) als Liste im Bericht.
- **Ungeklärte Fälle** stehen seit N6 in der Tabelle `migrationsprotokoll`, nicht mehr nur im Log. Beides auswerten.
- **Fixture:** `tests/fixtures/schema_p10.sql` ist ein Altstand vor P11; `scripts/p11_nachzug_probe.py` zeigt, wie ein synthetischer Altbestand aufgebaut wird. Wiederverwenden, nicht neu erfinden. Ein zweiter Altstand ohne `schema_version`-Tabelle (Stand der alten App, Version 0) muss ebenfalls durchlaufen.
- **Ausgabe:** zusätzlich zur JSON eine lesbare Zusammenfassung auf stdout (Summen je Sparte/Jahr alt/neu, Abweichungen, Bereiche, ungeklärte Fälle, Migrationsprotokoll).
- **Bericht:** `docs/neubau/berichte/P61-runde1.md`, Testausgabe `docs/neubau/berichte/P61-tests.txt`.
