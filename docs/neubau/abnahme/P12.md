# Abnahmeprotokoll P12: Auslagen und Ausgleich mit Zuordnungen

Abnahme durch Claude Fable, 10. September 2026. Lieferung von Codex (`gpt-6-astra`, medium),
Worktree `wt-p12`. Bericht: `docs/neubau/berichte/P12-runde1.md`.

## Geprüft

| Prüfung | Ergebnis |
|---|---|
| Gesamte Testsuite, selbst ausgeführt | **185 Tests, OK**, 1 übersprungen (P13-Stand: 179) |
| Bereichsprüfung an allen vier neuen Endpunkten (`GET /auslagen`, `GET /ausgleiche`, `POST /ausgleiche`, `DELETE /ausgleiche/{id}`) | `BereichDep` an jedem, `pruefe_sparte` an jeder übergebenen Sparte |
| Migration `005_auslagen_ausgleich.sql` | Nummer kollisionsfrei (P11 = 004, P13 = 006) |
| Scope | `app/auslagen.py`, `app/routers/auslagen.py`, `app/wiederholung.py` neu; Änderungen an `bewegungen.py`, `konten.py`, `buchungen.py` im erwarteten Rahmen |
| Auth, Health, Schema, Betriebsstatus | unverändert bereichsfrei |

## Beanstandet und behoben

`docs/superpowers/plans/` und `docs/superpowers/specs/` lagen im Worktree — Planungsdateien des
Werkzeugs, die nicht ins Projektarchiv gehören. Entfernt. Dieselbe Beanstandung gilt für P14 und
P15 und ist dort in den Rückläufer aufgenommen.

## Offen zur Sammelprüfung

Die Migrationsprobe auf einer Kopie der Produktionsdatenbank läuft nach dem Merge aller
M1-Pakete gemeinsam (Migrationen 001–008 in einem Durchlauf, Summen je Sparte und Jahr vorher
und nachher).

## Status

**Bestanden.** Merge in `neubau`.
