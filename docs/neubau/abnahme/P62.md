# Abnahme P62 — Umstellung auf den Neubau mit Rückweg (Ablauf und Prüfskript)

Datum: 11. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Opus als Subagent, Bericht `berichte/P62-runde1.md`.
Zweig `pkt/p62-umstellung`, Commit `5db976f`. Keine Migration, **keine Produktionsänderung**.

## Ergebnis: bestanden. Die Umstellung selbst ist nicht ausgeführt.

## Geliefert

- `scripts/umstellung_pruefung.py`: rein lesend (`mode=ro`, Prüfsummen vorher/nachher), nutzt die P61-Funktionen;
  weist WAL/Journal-Quellen, UNC-Pfade, die Live-Datei und `FINANZ_DB`-Pfade ab; Sitzungs-Cookie nur aus
  `FINANZ_SESSION_COOKIE`; Exitcodes 0/1/2. 17 Tests.
- `docs/BETRIEB-UND-ARCHITEKTUR.md` Abschnitt 11 „Umstellung auf den Neubau“: Sicherung, Migrationsprobe, Freigabe,
  Reihenfolge auf CT 101 inklusive Frontend-Umschaltung und Modellwechsel (P43c), Nachzug, Prüfungen, Abschluss,
  Rückweg.
- `app/main.py`: Root-Mount wählt per `FINANZ_FRONTEND=neu` das neue Frontend (Standard bleibt Studio); die neun
  Anmelde-/Passwortseiten werden immer aus `static-studio` ausgeliefert, weil ihre Pfade in `app/auth.py` fest
  verdrahtet sind. `/neu` und `/studio` unverändert. Umstellung und Rückweg sind damit ein systemd-Drop-In.

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Diff `app/main.py` gelesen | Auth-Seiten vor dem Root-Mount als eigene Routen, Mount-Wahl reine Funktion; ohne Variable identisches Verhalten |
| Suite des Agenten | 430 Tests grün (vor P51-Merge) |
| Gesamtsuite nach Merge mit P51 und Test-Nachzug | **451 Tests, OK, 1 übersprungen** |
| Browserprüfung des Agenten | drei Instanzen: Standard Studio unter `/`; mit `FINANZ_FRONTEND=neu` Neubau unter `/`, Studio unter `/studio`; ohne Bypass 303 auf `/login.html`, Seite lädt |
| Geheimnisse | keine |

## Offen (aus dem Bericht, vom Kopf bestätigt)

1. Umstellung auf CT 101 nicht ausgeführt; eigene, vom Nutzer freigegebene Sitzung nach Abschnitt 11.
2. Zahlenvergleich nur synthetisch; der echte Lauf gehört in den Umstellungsablauf (11.6.2).
3. Login-Sprung nach `/` mit dem Neubau nur aus dem Root-Mount abgeleitet, nicht mit echtem Passwort durchgeklickt
   (11.6.3, Sache des Nutzers).
4. P43c: Gewinnermodell im Ablauf als Platzhalter, nach dem Modellvergleich eintragen.
5. Der Befehl für die „nachher“-Kopie ist auf CT 101 noch nie gelaufen; beim ersten Mal Schritt für Schritt.
6. Suite-Eigenheit (nicht P62): zweiter Lauf auf derselben `FINANZ_DB`-Datei verfälscht `test_auto_kategorien`;
   Wegwerfdatei vor jedem Lauf löschen, wie es die Kopf-Läufe tun.
