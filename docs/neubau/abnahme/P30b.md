# Abnahme P30b — Gerüst-Lücken in `static-neu`

Datum: 10. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet als Subagent (Codex gesperrt),
Bericht `berichte/P30b-runde1.md`. Zweig `pkt/p30b-geruest-luecken`, Worktree `wt-p30b`,
Commits `96cd939` (Sonnet) und `e6fafa2` (Kopf-Nachzug). Keine Migration, keine Backend-Änderung.

## Ergebnis: bestanden

## Geliefert

- `app.js`: `state.filter` (Jahr, Sparte, Kategorie) persistent in `localStorage`, zentrales `setFilter()`
  rendert die aktive Seite neu; `#year-select`, `#sparte-select` und Sidebar-Sparten hängen daran;
  `drawKategorieFilter()` befüllt `#filter-kategorie` aus `/api/kategorien` je Sparte, mit Cache je Bereich/Sparte.
  Alias-Felder `state.year`/`state.sparteId` bleiben synchron, damit die sechs Seiten nicht umgebaut werden mussten.
- `ui.js`: `wizard({title, steps, onFinish})` auf dem vorhandenen `<dialog>`; Schritte mit `render`, `validate`
  (async, Fehler im Dialog), `lockBack`, `ctx.finishNow`.
- `konten.js`: Kassa-Zählung auf `wizard()` umgestellt (einziger echter Mehrschritt-Ablauf im Bestand).
- `uebersicht.js`, `export.js`, `buchungen.js`: lokale Kompensationen entfernt, sonst doppeltes Rendern.
- `tests/test_static_neu.py`: fünf neue Tests.

## Kopf-Nachzug

Der zentrale Kategoriefilter lud ohne `nur_aktive`, `buchungen.js` hatte vorher `nur_aktive=true` gesetzt.
Stillgelegte Kategorien wären im Filter aufgetaucht. Nachgezogen in `e6fafa2`.

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Diff gelesen | Gerüst-Code sauber, Seitenänderungen auf das Nötige begrenzt, Kommentare erklären die Entkopplung |
| Gesamtsuite, eigener Lauf, `FINANZ_DB` auf Wegwerfdatei, ohne Auth-Bypass | **367 Tests, OK, 1 übersprungen** (332 s) |
| App gestartet (Port 8032, Wegwerf-DB, `FINANZ_INSTANZ=test`, vom Nutzer gestartet, weil die Sicherheitsprüfung der Claude-App den Start blockierte) | `/neu/` lädt, Banner TEST-INSTANZ, Konsole nur Favicon-404 |
| Sparten-Wechsel Kopf-Select | Übersicht rendert neu: „Filter aktiv: Privatvermietung“, nur eine Sparten-Kachel, Sidebar-Eintrag aktiv, `neu-sparte` gespeichert, Route bleibt `#/uebersicht` |
| Jahr-Wechsel | Handler vorhanden, `neu-jahr` gespeichert, KPIs neu gezeichnet |
| Kategoriefilter | Select befüllt (leere DB: nur „alle“), Handler vorhanden, Buchungsliste lädt nicht mehr doppelt |
| `wizard()` | Dialog öffnet, „Weiter“ → Schritt zwei mit „Zurück“ und „Abschließen“, Zurück im ersten Schritt ausgeblendet |
| Escape im Dialog (offener Punkt des Agenten) | echter Tastendruck über Playwright: Dialog schließt, `onFinish` wird nicht ausgelöst |
| Buchungsliste | lädt mit Tabelle, Kategorie-Handler zentral |
| Externe Quellen, Geheimnisse | keine |

## Nicht geprüft

- Handy-Ansicht nur durch den Agenten (375 px, Bottom-Navigation, Kassa-Wizard), nicht vom Kopf wiederholt.
- Kassa-Zählung end-to-end nur durch den Agenten (mit Differenz, ohne Differenz, Fehlerpfad 422); der Kopf hat
  den Wizard-Mechanismus isoliert geprüft.

## Freigegeben für

P32 (Sparte) und P33 (Kategorien) können auf diesem Gerüst starten. Sie sollen `state.filter.*` lesen und
`setFilter()` nicht umgehen. Escape-Verhalten und `#drill-close` bleiben Gerüst-Verantwortung.
