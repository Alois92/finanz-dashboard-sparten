# Abnahme P40 — Erfassen-Fluss mit Auto-Kategorie und Auslagen

Datum: 10. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet (Codex gesperrt), Bericht `berichte/P40-runde1.md`.
Zweig `pkt/p40-erfassen-fluss`, Worktree `wt-p40`. Keine Migration, kein Backend geändert.

## Ergebnis: bestanden

| Prüfung | Ergebnis |
|---|---|
| Gesamtsuite, eigener Standardbefehl ohne Auth-Bypass | **310 Tests, OK, 1 übersprungen**. Die sechs Auth-Fehlschläge aus dem Bericht kamen nur vom gesetzten `FINANZ_TEST_AUTH_BYPASS` im Lauf des Teams. |
| Eigene Tests | 12 in `tests/test_p40_erfassen.py` |
| Dateigrenzen | nur `static-neu/pages/erfassen.js`, neu `erfassen.css`, Test, Berichte |
| Browser Handy-Ansicht | Formular mit Richtung, Betrag (`inputmode`), Sparte, Text, Zahlungsart, Datum, Kategorie, Auslage-Schalter; Buchung 14,80 € bar gespeichert, in `GET /api/buchungen` sichtbar, Formular geleert, Toast, „Zuletzt erfasst“ aktualisiert; kein horizontaler Überlauf, keine Konsolenfehler |

## Befunde, nicht sperrend

1. Kategorievorschlag zeigt nur einen Treffer; „bis zu drei Treffer mit Pfeiltasten“ und die Regel-Herkunft fehlen, weil `POST /api/parse` keine Trefferliste liefert. Backend-Ergänzung für später (Kennung: Vorschlagsliste im Parse-Endpunkt).
2. Kontoauswahl mit mehreren Konten je Sparte nicht geprobt.
3. Beleg fotografieren ist Platzhalter bis P43.
