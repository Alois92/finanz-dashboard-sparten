# Abnahme P42 — Bankimport mit Zuordnung zu Bewegungen

Datum: 10. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet (Codex gesperrt), Bericht `berichte/P42-runde1.md`.
Zweig `pkt/p42-bankimport-zuordnung`, Worktree `wt-p42`. Keine Migration, kein Backend geändert (N4 hatte alles geliefert).

## Ergebnis: bestanden, Browserprobe folgt auf dem Integrationsstand

| Prüfung | Ergebnis |
|---|---|
| Gesamtsuite, eigener Standardbefehl | **306 Tests, OK, 1 übersprungen** |
| Eigene Tests | 8 in `tests/test_p42_bankimport.py`, mit der vorhandenen George-Fixture, Zuordnen über die N4-Endpunkte |
| Dateigrenzen | nur `static-neu/pages/bankimport.js`, neu `bankimport.css`, Test, Berichte, Aufzeichnungsskript `berichte/P42-record.py` |
| Browser | vom Team nicht möglich; der Kopf prüft die Seite nach dem Merge auf dem gemeinsamen Stand (`neubau`) zusammen mit Export und Buchungsliste |

## Befunde, nicht sperrend

1. `GET /api/bankumsaetze` liefert für verbuchte Umsätze weder Buchungstyp noch Regel-Herkunft; die Seite zeigt einheitlich „verbucht“. Backend-Ergänzung für später (Kennung P42b).
2. „Zuordnung lösen“ liefert bei über `/verbuchen` entstandenen Buchungen 409 (N4-Festlegung), als Toast sichtbar.
