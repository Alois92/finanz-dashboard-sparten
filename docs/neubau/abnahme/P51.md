# Abnahme P50b + P51 — Historie, Storno und Erstattung (Migrationen 016, 017), P43b

Datum: 11. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet in zwei Anläufen (erster Agent am
Kontingent abgebrochen, zweiter hat den Stand übernommen), Bericht `berichte/P51-runde1.md`; Test-Nachzug durch
einen dritten Sonnet-Agenten, Bericht `berichte/F-tests-017-runde1.md`. Zweige `pkt/p51-storno-erstattung`
(`a00c842`, `dd622f2`) und `fix/tests-migration-017` (`ab13cd3`).

## Ergebnis: bestanden

## Geliefert

- **Migration 016** `buchung_aenderung` (Historie je Buchung) und neuer Wert `beleg_uebernahme` für
  `request_wiederholung.art` (P43b, Tabelle neu angelegt und umkopiert). `_update_buchung` protokolliert Kopffelder und
  Zeilen; `GET /api/buchungen/{id}/verlauf`; Abschnitt „Verlauf“ im Bearbeiten-Dialog.
- **Migration 017** `buchung.original_id`, `buchung.storniert_am`, `buchungszeile.original_zeile_id`; Views `v_zeile`
  (mit `storniert_am`) und `v_einnahmen_ausgaben` (ohne stornierte). Endpunkte `stornieren`, `entstornieren`,
  `erstatten`, `GET /api/buchungen/{id}` mit Erstattungen und `netto_cent`. Buchungsliste: Storno-Aktionen,
  durchgestrichene Markierung, Erstattungs-Abschnitt mit Rest-Vorbelegung; Überschreitung wird abgewiesen.
- Ein gemeinsamer Commit für beide Teile (verzahnt), vom Kopf akzeptiert.

## Prozessregel 1: Migrationsprobe auf der Produktionskopie

Kopf-Lauf mit `scripts.migrationsprobe` gegen `finanz-prod-kopie-2026-09-10.db`, Bericht
`outputs/datenrettung/migrationsprobe-017-bericht-2026-09-11.json` (Z:):

| Prüfung | Ergebnis |
|---|---|
| Angewendet | 1–17, zweiter Lauf leer |
| Summen je Sparte und Jahr | gleich, Abweichungen leer |
| Anzahl Buchungen/Zeilen | 25/34 vorher und nachher |
| Integrität, Fremdschlüssel | ok, leer |
| Quelle | Prüfsumme unverändert |
| Bekannte Befunde | wie A2neu: eine fremde Zuordnung in „Gesamtuebersicht“, vier Buchungen ohne Umsatz |

Hinweis: Der Bericht meldet nach der Probe `kostenstorno_spalten.buchung = True`, weil `storniert_am` jetzt eine reguläre
Spalte aus Migration 017 ist. Die Prüfung in `scripts/migrationsprobe.py` stammt aus der Zeit vor P51 und ist damit
für dieses Feld überholt; als kleine Schuld notiert.

## Test-Nachzug

15 Bestandstests waren nach den Migrationen rot: 13 feste Migrationslisten und Versionsnummern, ein Test, der die
fehlende Verlauf-Route dokumentierte (gelöscht, `test_p50b_verlauf` deckt es ab), und `test_migrationsprobe`, das im
Fixture eine Spalte doppelt anlegte, die Migration 017 inzwischen selbst erzeugt. `app/migrate.py` und Migrationen
blieben unverändert; `old_schema()` in `test_bereiche_migration` entfernt jetzt zusätzlich die P51-Spalten, damit der
Vergleich Nachzug/Frischschema wieder aussagekräftig ist. Scope-Erweiterung vom Kopf akzeptiert.

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Diff gelesen | Migrationen 016/017, Router-Endpunkte, Views; keine Gerüstdateien |
| Gesamtsuite nach allen Merges, ohne Auth-Bypass | **434 Tests, OK, 1 übersprungen** |
| Browserprüfung (Agent, Port 8038) | Verlauf nach PUT, Storno hin und zurück mit Summenwirkung, Teil-Erstattung mit Rest, Übererstattung 422, Mobil 375 px, Konsole sauber |
| Browserprüfung (Kopf, Testinstanz aus `neubau`, Port 8037, bestehende Wegwerf-DB von Schema 15 automatisch auf 17 nachgezogen) | Storno: Zeile durchgestrichen mit Pille, Ausgaben 1.217,97 → 1.172,97, Toast; Zurücknehmen stellt Summe wieder her. Historie im Bearbeiten-Dialog und `GET …/verlauf` zeigen beide Einträge mit Grund. Erstattung 20,00 auf Kategorie „Erstattungen“ (richtung beides): Gegenbuchung als Einnahme, 15 Buchungen, Einnahmen +20,00. **Befund 1:** 422 mit strukturiertem Detail erschien als „[object Object]“, vom Kopf in `api.js` behoben (Detailtext aus `detail.detail`). **Befund 2:** Storno-Bestätigung und Grund laufen über native `confirm`/`prompt` statt eines App-Dialogs; funktional, aber uneinheitlich, als kleine Schuld notiert. Konsole ohne Fehler. |
| Externe Quellen, Geheimnisse | keine |

## Offen

- `scripts/migrationsprobe.py`: Kostenstorno-Prüfung für `buchung.storniert_am` ist seit 017 überholt (nur noch
  `buchungszeile`-Spalten sind ein Altbestandsmerkmal).
- Kein Browsertest für die Erstattung einer Einnahme (nur Unit-Test).
