# Abnahme P41 — Konten und Kassen, offene Auslagen, Ausgleich-Dialog

Datum: 10. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet (Codex war gesperrt), Bericht `berichte/P41-runde1.md`.
Zweig `pkt/p41-konten-ausgleich`, Worktree `wt-p41`. Keine Migration.

## Ergebnis: bestanden

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Gesamtsuite, eigener Standardbefehl | **307 Tests, OK, 1 übersprungen** |
| Eigene Tests | 9 in `tests/test_p41_konten.py` |
| Dateigrenzen | nur `static-neu/pages/konten.js`, neu `konten.css`, `tests/test_p41_konten.py`, Berichte; Backend eine Zeile: `GET /api/konten` liefert zusätzlich `hinweis` (war in `berechne_kontostand` vorhanden, wurde nicht durchgereicht) |
| Browser Desktop und Handy | Kontenkarten je Art, „kein Anker“ statt 0, Dialog „Anfangsstand ändern“ geöffnet, Anker 123,45 € gesetzt, Stand sofort sichtbar, Toast, keine Konsolenfehler |
| Offene Auslagen, Ausgleichs-Historie | Bereiche vorhanden, leer in der Wegwerf-DB |
| Geheimnisse, neue Abhängigkeiten | keine |

## Befunde, nicht sperrend

1. Wunsch an das Gerüst (aus dem Bericht): `ui.js` hat keinen generischen Mehrschritt-Dialog; die Seite baut ihn lokal. Beim nächsten Gerüst-Paket in `ui.js` heben.
2. Ausgleich-Dialog nur per Testclient geprobt, nicht mit echten offenen Auslagen im Browser. Beim Zusammenspiel mit P40 (Auslage erfassen) nachholen.
