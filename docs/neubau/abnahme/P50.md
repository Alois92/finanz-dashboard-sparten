# Abnahme P50 — Buchungsliste mit Suche, Filtern und Bearbeiten

Datum: 10. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet (Codex gesperrt), Bericht `berichte/P50-runde1.md`.
Zweig `pkt/p50-buchungsliste-bearbeiten`, Worktree `wt-p50`. Keine Migration, kein Backend geändert.

## Ergebnis: bestanden, Historie bewusst nicht gebaut

| Prüfung | Ergebnis |
|---|---|
| Gesamtsuite, eigener Standardbefehl | **307 Tests, OK, 1 übersprungen** |
| Eigene Tests | 9 in `tests/test_p50_buchungen.py` (Liste, Cursor, PUT mit Version und 409, Delete, `node --check`) |
| Dateigrenzen | nur `static-neu/pages/buchungen.js`, neu `buchungen.css`, Test, Berichte |
| Browserprüfung durch das Team | Suche, Filter-Chips, Bearbeiten-Dialog, Speichern, 409-Hinweis, 375 px; dabei ein Laufzeitfehler (`zeilen` undefiniert) gefunden und behoben |

## Entscheidungen

- **Historie (Verlauf je Buchung) nicht gebaut.** Die Karte verlangt einen Endpunkt und eine Migration; der Nachtrag verbietet beides für die Frontend-Pakete. Die Seite zeigt bei fehlendem Endpunkt einen Hinweis. Wird als eigenes Backend-Paket nachgezogen (Kennung P50b: `GET /api/buchungen/{id}/verlauf`, Protokollierung bei PUT).

## Befund am Bestandscode, in die Schuldenliste

- **B7 (neu):** `_update_buchung` in `app/routers/buchungen.py` behandelt `kontakt_id` und `person_id` nicht über `model_fields_set` wie `bankkonto_id`/`bankumsatz_id`. Ein PUT ohne diese Felder setzt einen vorhandenen Kontakt auf NULL. Vom Team per Test belegt. Vor P50b beheben; der Bearbeiten-Dialog lässt Kontakt/Person deshalb vorerst unverändert.
