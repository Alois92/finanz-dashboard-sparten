# Abnahme P73 — Mobil-Navigation und Handy-Fixes

Datum: 12. September 2026 (nachts). Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet als Subagent, Bericht
`berichte/P73-runde1.md`, Karte `pakete/P73-mobil-navigation.md`. Zweig `pkt/p73-mobil-navigation`, Commits
`a97ce4b`, `9310c19`, `e822721`, Merge `bacb18b`. Nur Frontend, keine Migration, keine Abhängigkeit.

## Ergebnis: bestanden

## Geliefert

- **QA5-02:** sechster Bottom-Nav-Eintrag „Mehr“ öffnet ein Sheet mit allen Routen, die nicht in der Bottom-Nav
  sind (aus der `routes`-Map abgeleitet, künftige Seiten erscheinen automatisch), Bereichswahl, Theme, Passwort
  ändern, Abmelden; Rolle `dialog`, Fokus beim Öffnen, Schließen per Eintrag, Knopf und Escape.
- **QA5-01:** Erfassen-Grid unter 760 px einspaltig (`min-width:0` plus `!important` gegen das Inline-Style aus
  `erfassen.js`), `dialog` auf `max-width: calc(100vw - 16px)`.
- **QA5-03/04:** Tap-Ziele in Listen ≥ 40 px unter 760 px, Scroll-Hinweis (Verlaufsschatten) an scrollbaren Tabellen.
- Statische Prüfung `tests/test_p73_mobil.py` (10 Tests), Messtabelle des Agenten: alle 11 Seiten bei 320/375/390 px
  ohne horizontalen Überlauf.

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Diff `app.js`/`index.html` gelesen | Sheet-Logik wiederverwendet `ui.sheet`, Abmelden wortgleich zur Sidebar, keine Hardcodierung der Routen |
| Suite des Agenten | 594 Tests, OK (`berichte/P73-tests.txt`) |
| Browser (Kopf, Instanz 8051, 375×812, `clientWidth` = 375) | Erfassen: `scrollWidth` = `clientWidth` = 375, Media-Query greift; Bottom-Nav sechs Knöpfe 63×65 px (Plus 48×48). „Mehr“ öffnet das Sheet mit sparte/konten/kredit/kategorien/export/betrieb, Bereichswahl, Theme, Abmelden; Klick auf Betrieb schließt das Sheet und öffnet `#/betrieb` ohne Überlauf. Konsole ohne Fehler. |

## Offen

- Tap-Ziele in echten Listen (Buchungen, Kategorien) nur synthetisch gemessen.
- Dialoge „Zuordnen“, „Beleg prüfen“, „Jahr bestätigen“ auf dem Handy weiterhin ungeprüft (Testdaten).
