# Abnahme P72 — Betriebsseite im Neubau mit manueller Sicherung

Datum: 11. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet als Subagent (Plan vorab vom Kopf
freigegeben), Bericht `berichte/P72-runde1.md`, Karte `pakete/P72-betriebsseite.md`. Zweig `pkt/p72-betriebsseite`,
Commits `169aa31`, `e9c272a`, `2d0f5e8`, Merge `7f31162`. Keine Migration, keine neue Abhängigkeit.

## Ergebnis: bestanden

## Geliefert

- `GET /api/betrieb/uebersicht`: Schema, Sicherung (aus derselben Logik wie `/api/betrieb/status`, ausgelagert nach
  `betrieb.baue_sicherung_und_schema_status`), Ollama-Erreichbarkeit (wiederverwendet `auswertung_status`), KI-Vorschlag
  an/aus, Frontend, Instanz, Auswertungswarteschlange. Keine Pfade, Cookies oder Geheimnisse (Test prüft das).
- `POST /api/betrieb/sicherung`: dieselbe `sichere_datenbank()` wie im Lifespan; 409 bei laufender Sicherung
  (`sicherungs_lock.locked()`, bewusst einfacher Check), 503 über die bestehende Schreibschutz-Middleware.
- Seite `#/betrieb` (`static-neu/pages/betrieb.js/.css`), Sidebar-Eintrag „Betrieb“; Bottom-Nav unverändert.
- 8 Tests, `tests/test_bereiche.py` um die neue Ausnahme ergänzt.

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Diff Backend gelesen | Statuslogik einmal vorhanden, `main.py` ruft sie; Ollama-Check nicht dupliziert; keine Geheimnisse |
| Suite des Agenten | 569 Tests, OK (`berichte/P72-tests.txt`) |
| API auf Testinstanz 8051 (Prod-Kopie) | Übersicht liefert Schema 17, Sicherung vom 11.09., Ollama `qwen3.5:4b` erreichbar, KI an, Frontend `neu`, Instanz `test`, Warteschlange 0/0/0 |
| Browser (Kopf, Instanz 8051) | `#/betrieb` zeigt Datenbank (Schema 17, keine Migrationen, Lesen/Schreiben), Sicherung (11.09., db ok, 6 Belege fehlend in der Kopie, Zweitziel nicht konfiguriert), Foto-Auswertung (qwen3.5:4b, erreichbar, Warteschlange 0/0), KI an, Instanz test/Frontend neu. „Jetzt sichern“ → Toast „Sicherung abgeschlossen.“, Konsole ohne Fehler. |

## Offen

- Sidebar (und damit Betrieb, Export, Sparte, Konten, Kredit, Kategorien) ist unter 760 px nicht erreichbar —
  vorbestehende Lücke, wird in **P73 Mobil-Navigation** behoben (QA5-02).
- 409-Check ist check-then-act; für eine manuelle Admin-Aktion akzeptiert.
