# Abnahme P60b, P31b, P32b — Kleinkram aus den Frontend-Abnahmen

Datum: 11. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Claude Sonnet als Subagent, Bericht
`berichte/P60b-P31b-P32b-runde1.md`. Zweig `pkt/p60b-p31b-kleinkram`, Worktree `wt-p60b`, Commits `97c89c7`, `1ebb41a`,
`f02ae8f` (Sonnet) und `f37fc61` (Kopf: Alt-Test nachgezogen). Keine Migration.

## Ergebnis: bestanden

## Geliefert

- **P60b:** `GET /export/bericht` braucht `jahr` nur noch ohne `profil_id` (`app/routers/export.py`); der Ausschluss-Dialog
  in `pages/export.js` holt Buchungen seitenweise über den Cursor statt mit Limit 1000.
- **P31b:** `rechenbasis.hinweise()` liefert je Hinweis `wert_cent` als Zahl und einen Text im Euro-Format
  („50,99 €“ statt „5099 Cent“); `uebersicht.js` zeigte bereits `text`, keine Frontend-Änderung nötig.
- **P32b:** `#/sparte/gruppe-<id>` gewinnt auch bei gewählter Sparte im Kopf; beim Betreten wird die Sparte geräumt
  (Kopf und Sidebar „Alle Sparten“), eine spätere Sparten-Wahl im Kopf entfernt das Gruppen-Suffix.

## Entscheidung des Kopfs

`tests/test_p60_export.py` enthielt einen Test, der die alte Lücke (422 ohne `jahr`) ausdrücklich dokumentierte. Der Agent
durfte die Datei nicht ändern und hat den Fehlschlag gemeldet. Der Test ist auf das neue Verhalten (200) umgeschrieben.

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Diff gelesen | `export.py` (2 Zeilen), `rechenbasis.py` (Euro-Text, `wert_cent`), `export.js` (Cursor-Schleife), `sparte.js` (Gruppenzustand); nichts sonst im Gerüst |
| Modultests nach Test-Nachzug | `test_p60_export`, `test_p60b_export`, `test_p31b_hinweise`, `test_p31_uebersicht` grün |
| Gesamtsuite im Worktree, ohne Auth-Bypass | **413 Tests, OK, 1 übersprungen** |
| Browserprüfung (Agent, Port 8039) | Bericht mit Profil ohne Jahr 200; Ausschluss-Dialog über zwei Cursor-Seiten vollständig; Hinweis „Größte Buchung: 12,10 €.“; Gruppen-Hash mit gewählter Sparte springt auf die Gruppe, Kopf-Wahl entfernt Suffix; keine Konsolenfehler |
| Externe Quellen, Geheimnisse | keine |

Eigene Browserprüfung des Kopfs folgt stichprobenartig auf der gebündelten Instanz zusammen mit P51.
