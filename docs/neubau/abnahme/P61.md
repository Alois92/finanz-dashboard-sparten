# Abnahme P61 — Migrationsprobe-Skript (vorgezogen nach P20)

Datum: 10. September 2026. Abnehmer: Fable (Kopf). Umsetzung: Codex (`gpt-6-astra`, medium), Bericht `berichte/P61-runde1.md`.
Zweig `pkt/p61-migrationsprobe`, Worktree `wt-p61-migrationsprobe`. Keine Migration, keine Änderung an Anwendung oder Schema.

## Ergebnis: Skript bestanden. Die Probe auf der echten Produktionskopie steht noch aus (A2neu).

## Eigene Prüfung

| Prüfung | Ergebnis |
|---|---|
| Gesamtsuite, eigener Standardbefehl | **275 Tests, OK, 1 übersprungen** |
| Codex' Nachweise | 13 P61-Tests grün, vier synthetische Proben (P10-Altstand und Version 0, je zweimal) mit identischem Ergebnis, Quellen per SHA256 unverändert |
| Eigener Lauf gegen echte Datei (NAS-Kopie `Finanzdaten/finanz.db` vom 27. Juli, nur 1 Buchung) | Prüfungen bestanden, Abweichungen leer, ZINA Bereich 2, alle anderen Bereich 1, 14 Migrationen, zweiter Lauf leer, Fremdschlüssel und Integrität ok, Quelle vorher/nachher gleiche Prüfsumme |
| Schutz | `FINANZ_DB` als Quelle oder Ziel gesperrt, UNC-Quellen gesperrt, Quellen mit offenem WAL/Journal abgewiesen, keine Importe von `app.db`/`app.backup` |
| Geheimnisse, echte Namen, neue Abhängigkeiten | keine |

## Aufruf für die echte Probe

```
cd C:\Users\lblet\dev\finanz-dashboard-sparten
.venv\Scripts\python.exe -m scripts.migrationsprobe <lokale Kopie der Sicherung aus CT 101> --arbeitsordner %TEMP%\probe-prod
```

Die Kopie vorher von `Z:` auf eine lokale Platte kopieren (UNC-Sperre). Ergebnis in `docs/neubau/abnahme/A2neu-migrationsprobe.md` festhalten.

## Befund aus dem Juli-Lauf, vor der Prod-Probe zu beachten

Migration 003 meldet im Log „Entfernte fremde Zuordnungen auswertungsgruppe_sparte: 1“: eine Auswertungsgruppe enthielt eine Sparte des anderen Bereichs, die Zuordnung wurde still entfernt. Auf der Produktionskopie ist diese Zahl zu prüfen und, falls größer null, dem Nutzer zu nennen. Das ist der Kern von Schuld **A2** (lauter Abbruch statt stiller Bereinigung); das Skript macht die Zahl jetzt sichtbar, ändert die Migration aber nicht.
